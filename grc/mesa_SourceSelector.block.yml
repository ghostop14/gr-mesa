# auto-generated by grc.converter

id: mesa_SourceSelector
label: Source Selector
category: '[mesa]'

parameters:
-   id: holdTime
    label: Input Hold Time (s)
    dtype: float
    default: '10.0'

inputs:
-   domain: message
    id: in1
-   domain: message
    id: in2
    optional: true
-   domain: message
    id: in3
    optional: true
-   domain: message
    id: in4
    optional: true

outputs:
-   domain: message
    id: inputport
    optional: true

templates:
    imports: import mesa
    make: mesa.SourceSelector(${holdTime}, 4, 1,6144)

documentation: |-
    This block monitors the message inputs for a meta tag "decisionvalue" associated with the input data.  Whichever input has the maximum power is the one whose data is sent to the output.  A hold-down timer is available to limit "bouncing".

file_format: 1
