<?xml version="1.0"?>
<block>
  <name>Normalize</name>
  <key>mesa_Normalize</key>
  <category>[mesa]</category>
  <import>import mesa</import>
  <make>mesa.Normalize($vecsize)</make>
	<param>
		<name>Size</name>
		<key>vecsize</key>
		<value>1024</value>
		<type>int</type>
	</param>

  <sink>
    <name>in</name>
    <type>float</type>
    <vlen>$vecsize</vlen>
  </sink>

  <source>
    <name>out</name>
    <type>float</type>
    <vlen>$vecsize</vlen>
  </source>

  <doc>
  This block will take an input vector and normalize the results over the vector.  (This block is an adaptation of the RA Vector Normalize block that had some hard-coded values preventing proper use)
  </doc>
</block>
