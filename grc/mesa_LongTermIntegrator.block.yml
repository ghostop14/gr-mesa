# auto-generated by grc.converter

id: mesa_LongTermIntegrator
label: Long Term Integrator
category: '[mesa]'

parameters:
-   id: vlength
    label: Vector Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlength == 1 else 'none' }
-   id: normalize
    label: Normalize Output
    dtype: enum
    options: ['True', 'False']
    option_labels: ['Yes', 'No']
-   id: reset
    label: Reset
    dtype: bool
    default: 'False'

inputs:
-   domain: stream
    dtype: float
    vlen: ${ vlength }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ vlength }
-   domain: message
    id: runtime
    optional: true

templates:
    imports: import mesa
    make: mesa.LongTermIntegrator(${vlength},${normalize})
    callbacks:
    - reset(${reset})

documentation: |-
    This block will take an input stream-to-vector (vlen/fftsize), and performs continuous vector aggregation.  The block will output a message few seconds containing the runtime as a string.

    NOTE: A snapshot period of zero disables snapshots.

file_format: 1
