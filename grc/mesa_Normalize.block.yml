# auto-generated by grc.converter

id: mesa_Normalize
label: Normalize
category: '[mesa]'

parameters:
-   id: vecsize
    label: Size
    dtype: int
    default: '1024'

inputs:
-   domain: stream
    dtype: float
    vlen: ${ vecsize }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ vecsize }

templates:
    imports: import mesa
    make: mesa.Normalize(${vecsize})

documentation: |-
    This block will take an input vector and normalize the results over the vector.  (This block is an adaptation of the RA Vector Normalize block that had some hard-coded values preventing proper use)

file_format: 1
