# auto-generated by grc.converter

id: mesa_AutoCorrelatorSink
label: Fast Auto-Correlator Sink
category: '[mesa]'

parameters:
-   id: sampRate
    label: Sample Rate
    dtype: float
    default: samp_rate
-   id: fac_size
    label: FAC Size
    dtype: int
    default: '512'
-   id: fac_decimation
    label: FAC Decimation
    dtype: int
    default: '10'
-   id: useDB
    label: Output
    dtype: enum
    options: ['False', 'True']
    option_labels: [Normalized, dB]
-   id: title
    label: Title
    dtype: string
    default: '""'
-   id: grid
    label: Show Grid
    dtype: enum
    options: ['True', 'False']
    option_labels: ['Yes', 'No']
-   id: autoScale
    label: Auto-Scale
    dtype: enum
    options: ['False', 'True']
    option_labels: ['No', 'Yes']
-   id: yMin
    label: Y Min
    dtype: float
    default: '0'
-   id: yMax
    label: Y Max
    dtype: float
    default: '1'
-   id: gui_hint
    label: GUI Hint
    dtype: gui_hint
    hide: part

inputs:
-   domain: stream
    dtype: complex

templates:
    imports: import mesa
    make: |-
        <%
            win = 'self._%s_win'%id
        %>\
        mesa.AutoCorrelatorSink(${sampRate}, 
            ${fac_size}, 
            ${fac_decimation}, 
            ${title}, 
            ${autoScale}, 
            ${grid}, 
            ${yMin}, 
            ${yMax}, 
            ${useDB}
        )
        self._${id}_win = self.${id}.getWidget()
        ${gui_hint() % win}

documentation: |-
    This block is a QT GUI replacement for the fast auto-correlation block previously available in gr-baz.  It uses the Wiener Khinchin theorem that the FFT of a signal's power spectrum is its auto-correlation function.  FAC Size controls the FFT size and therefore the length of time (samp_rate/fac_size) the auto-correlation runs over.  While a separate block provides an all-in-one sink, this can be fed into a vector-to-stream then into a time sink block for display.

file_format: 1
