# auto-generated by grc.converter

id: mesa_AvgToMsg
label: Average To Msg
category: '[mesa]'

parameters:
-   id: veclen
    label: Vector Length
    dtype: int
    default: '1024'
    hide: ${ 'part' if veclen == 1 else 'none' }
-   id: hold
    label: Lock
    dtype: bool
    default: 'False'

inputs:
-   domain: stream
    dtype: float
    vlen: ${ veclen }

outputs:
-   domain: message
    id: avg

templates:
    imports: import mesa
    make: |-
        mesa.AvgToMsg(${veclen})
        self.${id}.setHold(${hold})
    callbacks:
    - setHold(${hold})

documentation: |-
    This block will output the single scalar float average of the input floats as a message.

file_format: 1
