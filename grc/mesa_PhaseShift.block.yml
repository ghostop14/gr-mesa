# auto-generated by grc.converter

id: mesa_PhaseShift
label: Phase Shift
category: '[mesa]'

parameters:
-   id: shift_in_radians
    label: Phase Shift (rad)
    dtype: float
    default: '0.0'

inputs:
-   domain: stream
    dtype: complex
-   domain: message
    id: shift_rad

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import mesa
    make: mesa.PhaseShift(${shift_in_radians})
    callbacks:
    - setShift(${shift_in_radians})

documentation: |-
    This block will phase shift the input signal by the specified number of radians by multiplying the input times a shift value: gr_complex(cos(d_shift_in_radians),sin(d_shift_in_radians))

file_format: 1
